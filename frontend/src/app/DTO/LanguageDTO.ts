
export class LanguageDTO {
  name: string; // name of the language/framework
  isFramework: boolean;
  isDatabase: boolean;
  area: string; // front-end / back-end
  icon: string; // src should come from `assets` folder
}

